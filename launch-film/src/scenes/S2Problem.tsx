/* Beat 2 — Problem statement. Type only; absolutely still. */
import React from 'react';
import { Statement } from '../components/Statement';

export const S2Problem: React.FC = () => (
  <Statement lines={['Bye bye,', 'guesswork.']} />
);
